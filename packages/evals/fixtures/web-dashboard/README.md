# RepoPilot Web Dashboard Fixture

This fixture repository represents a small dashboard surface for benchmark tasks. It is intentionally minimal and provides the paths that UI, docs, and local-mode labeling tasks expect.

## Local Mode

Local mode records draft PR metadata without writing to GitHub. Real write mode requires verified GitHub App credentials and an approved, validated, security-scanned run.

